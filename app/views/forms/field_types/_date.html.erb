<!-- date -->
<div class="input-group date" ng-show="(field.field_type == 'date')">
  <span class="input-group-addon"><span class="glyphicon glyphicon-calendar"></span></span>
  <input type="text" class="form-control datepicker" ng-if="field.field_type == 'date'" name="{{ field.name }}">
</div>