<!-- number -->
<input type="number" class="form-control" ng-if="field.field_type == 'number'" name="{{ field.name }}"  max="{{ field.properties.to_number }}" min="{{ field.properties.from_number }}">