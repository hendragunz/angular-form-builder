<!-- rating -->
<%= f.input question.id.to_s, as: :inline_radio_buttons, label: question.field_label, hint: question.field_hint, collection: build_rating_options(question), required: question.required, item_wrapper_class: "#{question.properties['format'] == 'inline' ? 'radio-inline' : ''}" %>